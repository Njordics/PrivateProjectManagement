services:
  project-manager:
    build: .
    container_name: project-manager
    user: "${UID}:${GID}"
    ports:
      - "51001:51001"
    volumes:
      - ./data:/app/instance
    restart: unless-stopped
